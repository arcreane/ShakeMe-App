name: Deploy MAUI App to Staging

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build App
        run: dotnet build --configuration Release

      - name: Simulate staging deployment
        run: echo "📱 Déploiement MAUI STAGING (à personnaliser)"
